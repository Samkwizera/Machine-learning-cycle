events {}

http {
    # Docker's embedded DNS. Re-resolving per request lets nginx spread load
    # across all `api` replicas created by `docker compose --scale api=N`.
    # Short TTL: nginx reuses one cached replica address for the whole TTL, so a
    # long one parks every in-flight request on a single replica.
    resolver 127.0.0.11 valid=1s ipv6=off;

    server {
        listen 8000;

        location / {
            set $backend "http://api:8000";
            proxy_pass $backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_connect_timeout 5s;
            proxy_read_timeout 30s;
        }
    }
}
